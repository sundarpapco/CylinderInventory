<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="18dp"
        android:layout_marginTop="38dp"
        android:text="Unapproved cylinders detected"
        android:textColor="@color/colorAccent"
        android:fontFamily="sans-serif-condensed"
        android:textSize="17sp"
        android:maxLines="1"
        android:ellipsize="end"/>

    <RadioGroup
        android:id="@+id/allotment_conf_radio_group"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="18dp"
        android:layout_marginTop="28dp">

        <RadioButton
            android:id="@+id/radio_button_allot_later"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Approve it later"
            android:textColor="@android:color/black"
            android:textSize="19sp"
            android:fontFamily="sans-serif-condensed"/>

        <RadioButton
            android:id="@+id/radio_button_ignore"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Ignore unapproved cylinders"
            android:textColor="@android:color/black"
            android:textSize="19sp"
            android:fontFamily="sans-serif-condensed"
            android:layout_marginTop="6dp"/>
    </RadioGroup>

    <Button
        android:id="@+id/unalloted_conf_btn_approve"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="APPROVE"
        android:backgroundTint="@color/colorAccent"
        android:textColor="@android:color/white"
        android:layout_marginTop="46dp"
        android:layout_marginEnd="18dp"
        android:layout_marginBottom="16dp"
        android:layout_gravity="end"/>

</LinearLayout>